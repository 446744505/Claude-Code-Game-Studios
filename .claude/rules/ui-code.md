---
paths:
  - "src/ui/**"
---

# UI 代码规范

- UI 不得持有或直接修改游戏状态，仅负责展示；通过命令或事件请求状态变更
- 所有 UI 文案必须经过本地化系统，禁止硬编码面向玩家的字符串
- 所有可交互元素须同时支持键鼠与手柄输入
- 所有动画须可跳过，并遵守用户对动态效果与无障碍的偏好设置
- UI 音效须经由音频事件系统触发，不得直接播放
- UI 不得阻塞游戏线程
- 文字缩放与色盲模式为必需能力，非可选
- 须在所支持的最小与最大分辨率下测试所有界面
